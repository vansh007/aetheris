from setuptools import setup, find_packages

setup(
    name="aetheris",
    version="1.0.0",
    description="Intelligent Air Quality Prediction & Advisory System",
    packages=find_packages(),
    python_requires=">=3.9",
)
